name: 22-basics-github-cli.yml
run-name: ${{github.actor}} explaining github cli - try number ${{github.run_number}}

on:
  workflow_dispatch:


jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello world
        uses: echo "hello world"
